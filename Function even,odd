#include<stdio.h>
int main()
{
        int N;
        printf("enter a number:");
        scanf("%d",&N);
        if(N%2==0)
          printf("It is even");
        else
            printf("It is odd");
        printf("\nfinished");
        return 0;
}
